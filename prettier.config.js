/** @type{import('prettier').Config} */
const config = {
  singleQuote: true,
  semi: false,
  arrowParens: "always",
  endOfLine: "lf",
  trailingComma: "none",
  bracketSpacing: true,
  printWidth: 80,
  useTabs: false,
  tabWidth: 2,
  plugins: [],
};

export default config;
